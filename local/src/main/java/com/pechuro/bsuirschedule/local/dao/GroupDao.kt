package com.pechuro.bsuirschedule.local.dao

import androidx.room.Dao

@Dao
interface GroupDao {
/*
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insert(vararg group: GroupDB)

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insert(groups: List<GroupDB>)

    @Query("DELETE FROM all_groups")
    fun delete()

    @Query("SELECT * FROM all_groups")
    fun getAll(): Single<List<GroupDB>>

    @Query("SELECT number FROM all_groups")
    fun getAllNumbers(): Single<List<String>>

    @Query("SELECT EXISTS(SELECT 1 FROM all_groups)")
    fun isAuditoriesNotEmpty(): Single<Boolean>*/
}