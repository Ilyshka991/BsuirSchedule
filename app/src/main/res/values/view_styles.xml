<resources>

    <!-- Button styles -->
    <style name="TextButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textSize">18sp</item>
        <item name="android:padding">8dp</item>
        <item name="android:textColor">?attr/buttonTextColor</item>
        <item name="rippleColor">?attr/buttonRippleColor</item>
    </style>

    <!-- Text styles -->
    <style name="MessageText" parent="Widget.MaterialComponents.TextView">
        <item name="android:gravity">center</item>
        <item name="android:textColor">?attr/textMessage</item>
        <item name="android:textSize">24sp</item>
    </style>
</resources>
